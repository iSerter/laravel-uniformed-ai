Prices may be different for smaller size payloads because  most of them define pricing differently for below and above 128K tokens.
I picked the highest prices to be on the safe side. Write that as a comment into the new migration we'll create. 


--- 

Model Name: `gpt-5.2`
Provider: `OpenAI`
Price: 
```
Input:
$1.750 / 1M tokens
Output:
$14.000 / 1M tokens
```


---

Model Name: `GPT-5.2-pro`
Provider: `OpenAI`
Price:
```
Input:
$21.00 / 1M tokens
Output:
$168.00 / 1M tokens
``` 

--- 

Model Name: `GPT-5-mini`
Provider: `OpenAI`

Price
Input:
$0.250 / 1M tokens
Cached input:
$0.025 / 1M tokens
Output:
$2.000 / 1M tokens


---

Provider: `openrouter`
Model: `x-ai/grok-4-fast` 
Price:
```
Input:
$0.40 / 1M tokens
Output:
$1 / 1M tokens
```

---

Provider: `openrouter`
Model: `x-ai/grok-4.1-fast` 
Price:
```
Input:
$0.40 / 1M tokens
Output:
$1 / 1M tokens
```


--- 

Provider: `openrouter`
Model: `google/gemini-3-pro-preview` 
Price:
```
Input: $4 / 1M tokens
Output: $18 / 1M tokens
```

--- 

Provider: `openrouter`
Model: `deepseek/deepseek-v3.2` 
Price:
```
Input: $0.56 / 1M tokens
Output: $1.68 / 1M tokens
```

--- 

Provider: `openrouter`
Model: `anthropic/claude-opus-4.5` 
Price:
```
Input: $5 / 1M tokens
Output: $25 / 1M tokens
```

--- 


Provider: `openrouter`
Model: `google/gemini-3-flash-preview` 
Price:
```
Input: $0.50 / 1M tokens
Output: $3 / 1M tokens
```

--- 


Provider: `openrouter`
Model: `anthropic/claude-sonnet-4.5` 
Price:
```
Input: $6 / 1M tokens
Output: $22.50 / 1M tokens
```

--- 


Provider: `openrouter`
Model: `openai/gpt-oss-120b` 
Price:
```
Input: $0.10 / 1M tokens
Output: $0.50 / 1M tokens
```

--- 


Provider: `openrouter`
Model: `anthropic/claude-haiku-4.5` 
Price:
```
Input: $1 / 1M tokens
Output: $5 / 1M tokens
```

--- 


Provider: `openrouter`
Model: `openai/gpt-5.2` 
Price:
```
Input: $1.75 / 1M tokens
Output: $14 / 1M tokens
```

--- 


Provider: `openrouter`
Model: `qwen/qwen3-max` 
Price:
```
Input: $3 / 1M tokens
Output: $15 / 1M tokens
```

--- 


Provider: `openrouter`
Model: `qwen/qwen3-235b-a22b-thinking-2507` 
Price:
```
Input: $0.65 / 1M tokens
Output: $3 / 1M tokens
```

--- 


Provider: `openrouter`
Model: `` 
Price:
```
Input: $ / 1M tokens
Output: $ / 1M tokens
```